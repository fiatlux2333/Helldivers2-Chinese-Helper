# 更新日志

## 0.6.3 - 2026-09-13

### 新增

- 发送/快捷喊话/战备触发支持中途取消：翻译与预览阶段可点“取消”，注入阶段由 Rust 侧取消令牌协同中止，取消后草稿保留。取消或发送的注入阶段（几百毫秒内）物理按键会正常透传给游戏：此时游戏聊天框处于打开状态，字符合落在聊天框里，属预期行为。
- 首键缓冲：按聊天键唤出侧栏后，HD2 约 1.1 秒抢前台闪断期间输入的字母/数字/标点会被缓冲，侧栏焦点恢复后自动回放，根治侧栏吞字。
- 游戏聊天键支持小键盘 Enter：键盘钩子用扩展键位标志区分主 Enter 与小键盘 Enter；小键盘 Enter 不能再录制为全局热键（注册时与主 Enter 冲突）。
- 聊天翻译多行并发请求（4 路）与本地缓存：相同原文与翻译设置直接命中缓存，不再重复请求。并发以一次最多 4 个请求换更低的总延迟；配额极低的接口若因此限流报错，欢迎反馈调整。
- 截图翻译 OCR 中英双路并发识别，按行择优。
- 战备宏新增统一延迟设置：一键把所有预设的菜单打开/按键保持/间隔延迟套用为同一组数值。
- 词库补充 `Spore Burst Strain`（孢裂变种）词条及其 `Spore Burst` / `Spore Strain` 别名。

### 修复

- 首键缓冲两处会“空转/丢键”的时序缺陷：①侧栏刚聚焦（闪断尚未发生）时的空回放会把缓冲整体解除武装，导致约 1.1 秒后的抢前台闪断根本不进缓冲；②回放被“每会话一次”的闩锁挡住，第二段闪断的按键无法回放。现在空回放保持缓冲武装（看门狗与会话结束各自负责停吞），每次焦点恢复都可回放，用户离开侧栏时立即停吞，回放注入失败（SendInput 全部被拒）时按键放回缓冲等待重试。游戏连续抢前台（闪断成串发生）同样覆盖：每次回放成功后续期捕获窗口，下一轮闪断继续被捕获，直到侧栏会话结束。
- 取消发送不再盲目向游戏补发 Esc：仅当游戏聊天框确实（或极可能）开着时才发送 Esc 关闭聊天框，避免翻译阶段取消时误开 HD2 暂停菜单；注入中取消落在“聊天框键已发出、文字未开始”窗口时同样补发 Esc，避免聊天框滞留在游戏里吞 WASD。四处取消路径统一走同一收尾逻辑。
- 修复取消请求落在“最后一个检查点之后、Rust 事务启动之前”竞态窗口时被静默丢弃的问题：注入调用在途时取消会在 Rust 侧挂起一个短时效待取消标记（500ms），下一个事务启动即中止；事务已自行结束时自动清除标记（含取消 IPC 晚到的竞态），不影响下一次正常发送。
- 取消请求晚到（注入已完成）时如实提示“取消晚了一步，注入已完成”，不再显示成功提示造成误解。
- 修复侧栏会话失败回落主窗口后首键缓冲继续吞键的问题：回落时直接丢弃缓冲。
- 修复按 Ctrl+小键盘 Enter 录制全局热键时会实际注册成 Ctrl+Enter 的问题：热键录制不再产出小键盘 Enter 令牌，战备录制遇到小键盘 Enter 时恢复给出可理解的提示。

## 0.6.2 - 2026-08-30

- 新增独立中文侧栏焦点守卫：HD2 打开聊天框后约 1.1 秒会短暂抢回前台吞掉正在输入的字符，守卫会在失焦后通过系统前台探测立即恢复侧栏焦点；恢复成功后自动重延保护期，二次闪烁同样覆盖。
- 远控软件注入的聊天键现在可以唤出侧栏：键盘钩子改为只忽略助手自身注入的事件（带内部标记），远控注入的按键与物理按键走相同校验；日志区分 `source=physical` / `remote_injected`。
- CapsLock 输入法保护故障（faulted）时降级放行：不再阻断发送、快捷喊话、取消侧栏和发送后回游戏，仅暂停自动切换并记录日志；切到其他窗口成功或手动恢复后保护自动重新启用。
- 侧栏窗口抢不到焦点时先尝试隐藏再显示以重置 Windows 前台锁资格，重试后仍失败才提示，错误文案不再断言单一原因。
- 新增失焦诊断埋点（距打开时长、实时前台归属）与键盘钩子来源标记，便于远程定位问题。
- 键盘钩子内不再执行磁盘写入与 IPC：Escape 日志与事件改由工作线程处理，聊天键来源随信号传递。
- 设置调整：聊天框文字焦点等待范围 300–1200ms → 100–1000ms，默认 500 → 300ms；文字输入间隔维持 10–30ms（默认 15ms）。
- 修复外部贡献者报告的侧栏 DOM 焦点在窗口未取得原生焦点时静默失败的问题（PR #7，感谢 @webbrain-one）。

## 0.6.1 - 2026-08-24

- 新增“恢复输入状态”入口：发送失败或输入异常时可一键释放可能残留的 Enter / Esc / Ctrl / Alt / Shift / Win 按键，并按当前前台窗口对账 CapsLock，原草稿保留。
- 新增发送 / 取消 / 快捷喊话 / 战备触发后的自动游戏态恢复（可在设置中关闭）：程序只校验游戏键盘布局状态，不再强制切换 Windows 键盘布局。
- CapsLock 输入法保护加入重试与失败暂停：切换失败连续达到上限时暂停自动切换并提示，手动恢复输入状态或重新开关设置即可恢复保护。
- 中文侧栏窗口启动更健壮：预配置窗口缺失时自动重建，显示后校验可见性，健康检查失败时保留窗口并在下次聊天键重试。
- 侧栏隐藏前先释放输入框 DOM 焦点和输入法组字，降低发送后 WASD 进入隐藏输入框的“虚空打字”问题。
- 注入报告细化到请求 / 成功事件数、文本事件数和最后错误码，诊断日志加入 Windows 版本、键盘布局和前后台完整性级别等环境信息。
- 截图翻译 OCR 解析增强：支持显式玩家 ID、`ID:数字`、`[玩家名]` 等前缀形式，减少说话人误判。
- 错误提示新增“恢复输入状态”快捷按钮，覆盖按键未释放、CapsLock 保护失败等场景。
- 战备默认时序微调：菜单打开延迟 120→100ms，按键保持 35→50ms，需按旧时序的用户可在战备配置中自行调整。

## 0.6.0 - 2026-08-16

- 新增独立 `chat-overlay` 中文侧栏窗口，主助手窗口不再反复改尺寸、装饰和任务栏状态，降低侧栏唤出、发送和重入时的焦点竞争。
- 中文侧栏支持直发 / 中译英切换、拖动位置持久化、提前预热显示和松开聊天键后再聚焦，改善吞开头字符、拖动后位置回退和任务栏闪现问题。
- 主界面发言与快捷喊话支持自动打开游戏聊天框；捕获后不再要求玩家先手动按 Enter 打开聊天框。
- 新增战备模块，可从战备库添加预设、手动配置方向序列、选择 WASD 或方向键输入，并为战备录制全局热键。
- 战备热键新增可选“允许数字单键热键”：默认关闭；开启后可绑定主键盘数字和小键盘数字，字母、Space、Tab、Ctrl、Alt、Shift 单键仍禁止。
- 英译中 / 中译英提示词改为按消息匹配官方词库候选，优先使用官方简体中文译名和英文原名，覆盖敌人、战备、武器等术语。
- 截图翻译加入 OCR 阶段诊断日志、亮度范围记录和失败阶段标记，便于排查“框选正确但翻译失败”。
- 错误提示新增面向普通用户的处理建议和快捷操作按钮，如重新捕获、重新校准、导出日志、打开设置、恢复输入焦点。
- 退出流程增加 CapsLock 恢复、单实例唤回和 1.5 秒强制退出兜底，降低关闭后进程残留导致再次启动无反应的问题。

## 0.5.4 - 2026-08-06

- 新增英译中聊天结果展示位置设置，可选择翻译完成后进入“聊天翻译”页，或留在游戏内透明 HUD 显示最近译文。
- 游戏内透明 HUD 新增水平和垂直位置设置，并按游戏可用区域保存相对位置。
- 关闭中文侧栏时不再最小化隐藏窗口，降低回车发送后 Windows 任务栏意外获得焦点的概率。
- 游戏文字注入默认改为逐字符 Unicode SendInput，并提供 10-30ms 输入间隔设置。
- GBK Alt 数字码在真实 HD2 中仍会被游戏忽略，现从普通设置中停用，旧配置自动迁移到 Unicode。
- Unicode 发送、快捷喊话和主界面填入会同步执行游戏态 CapsLock 保护，降低 HD2 中文输入法卡键风险。
- 取消侧栏输入的 Esc 流程改为等待按键释放、恢复并确认 HD2 前台稳定后再注入，失败时记录完整注入报告。
- 权限不兼容提示改为直接说明 Windows 高低权限限制，并提示优先让游戏和助手都普通运行，必要时两边都管理员运行。

## 0.5.3 - 2026-08-02

- 修复中文助手主界面和中文侧栏恢复时位置回退、离屏以及窗口尺寸切换闪烁的问题。
- 修复助手中按 Enter 后松键被误判为游戏聊天键，导致侧栏误弹出的问题。
- 修复侧栏拖动位置无法用于后续展开和唤回的问题。
- 增加“导出诊断日志”按钮，将运行日志复制到安装目录的 `HD2CN-diagnostic.log`。

## 0.5.2 - 2026-08-01

- 继续修复 OCR 丢失说话人和换行时的英文聊天拆分。
- 处理 `B ro`、`Letl s` 等典型 OCR 空格/大小写噪声，并识别末尾单词形式的敌人消息。
- 修复 OCR 未识别重复说话人时，把相邻的多条短消息误判成长消息换行的问题；现在会保留独立聊天行并继承已识别的说话人。
- 增加多条相邻聊天消息的回归测试，确保游戏中的五句连续聊天不会被合并成一句。

## 0.5.1 - 2026-08-01

- 修复 Windows OCR 将多条英文聊天粘成一条后只翻译成一句的问题。
- 对无说话人标记的粘连聊天按标点和常见消息起始词拆分，再逐条调用翻译接口。
- 清理 OCR 将英文撇号识别成 `《`、全角标点和标点前空格造成的噪声。

## 0.5.0 - 2026-08-01

- 新增启动时检查 GitHub 最新正式版，并支持在设置中手动检查更新。
- 更新提示只打开官方 Releases 页面，不会自动下载、安装或替换程序。
- NSIS 安装包加入 `必看说明.txt`，首次使用可直接查看离线流程。
- 补充更新检查的 IPC、语义版本比较和浏览器预览测试。
- 统一 0.5.0 版本号，并修正 Rust 格式检查差异。

## 0.4.0

- 新增游戏右侧中文输入侧栏、连续会话和快捷喊话。
